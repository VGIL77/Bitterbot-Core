# ENGRAM Neural Observatory Requirements
# For the dashboard WebSocket server

# Core web framework
aiohttp>=3.9.0
aiohttp-cors>=0.7.0

# WebSocket support
python-socketio>=5.10.0

# Async utilities
asyncio>=3.4.3

# The dashboard also requires the main backend dependencies
# Install from parent directory: pip install -r ../requirements.txt